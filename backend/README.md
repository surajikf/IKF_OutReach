# Backend

API app, Prisma ownership, background worker, deployment notes, and operational tools live here.

Key docs:

- `docs/ARCHITECTURE.md`
- `docs/DEPLOYMENT.md`
- `docs/PROJECT_LAYOUT.md`

Common commands:

```bash
npm install
npm run dev
npm run worker
npm run build
npm run start
npm run typecheck
npm run test
```

